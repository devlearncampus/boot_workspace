<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sinse.bootwebsocket.model.member.MemberMapper">

    <select id="selectAll" resultType="Member">
        select * from member
    </select>

    <select id="select" parameterType="int" resultType="Member">
        select * from member where member_id=#{member_id}
    </select>

    <select id="login" parameterType="Member" resultType="Member">
        select * from member where id=#{id} and password=#{password}
    </select>

    <insert id="insert" parameterType="Member">
        insert into member(id,password,name,email)
        values(#{id},#{password},#{name},#{email})
    </insert>

    <update id="update" parameterType="Member">
        update member set id=#{id}, password=#{password}, name=#{name}, email=#{email}
        where member_id=#{member_id}
    </update>

    <delete id="delete" parameterType="int">
        delete from member where member_id=#{member_id}
    </delete>
</mapper>